{
  "name": "venusp-desktop",
  "version": "1.0.0",
  "private": true,
  "description": "Build tools for protected VenusP desktop app (Neutralino)",
  "scripts": {
    "build:protected": "node scripts/build-protected.js",
    "build:desktop": "npm run build:protected && cd ../VenusP-Desktop && ../venusp-desktop/node_modules/.bin/neu build --release",
    "run:desktop": "cd ../VenusP-Desktop && ../venusp-desktop/node_modules/.bin/neu run"
  },
  "devDependencies": {
    "@neutralinojs/neu": "^11.7.2",
    "javascript-obfuscator": "^5.5.0"
  }
}
